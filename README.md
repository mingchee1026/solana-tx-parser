# Parser for Solana Transaction

After cloning the repo, in the project directory run `pnpm install`. Then:

```
pnpm start
```

For an example on how to use the library, you can check out: `src/cli.ts`.
